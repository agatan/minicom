let global_var0: int = 0;
print(global_var0);

def add(x: int, y: int): int {
    global_var0 = x + y;
    global_var0
};

let x: float = 1.0;
let y: float = 2.0;
let global_var1: int = add(0, 1) + 1;

print(global_var0);
print(global_var1);

def sub(x: int, y: int): int {
  global_var1 = x - y + 1;
  x - y
};

print(sub(4, 1));
print(global_var1);
