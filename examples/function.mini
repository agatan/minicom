let global_var0 = 0;

def add(x: int, y: int): int {
    global_var0 = x + y;
    global_var0
};

let x = 1.0;
let y = 2.0;
let global_var1 = add(1, 2);

def sub(x: int, y: int): int {
global_var1 = x - y;
  x - y
};

print(x);
print(y);
