sudo: required

services:
  - docker

script:
  - docker run -ti -v $(pwd):/app -w /app agatan/minivm-dev:latest sh -c "cargo build --verbose && cargo test -p minivm-basis -p minivm-syntax -p minivm"
